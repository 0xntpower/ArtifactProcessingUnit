#pragma once

#include <cstdint>
#include <string>

namespace apu {

    // Common type aliases for consistency
    using ArtifactId = std::string;  // UUID string
    using Hash = std::string;         // BLAKE3 hash string

} // namespace apu